fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pillow==11.3.0
pydantic==2.13.5
httpx==0.28.1
arq==0.28.0
redis==5.3.1

# ktp-schema (the shared contracts package in ../../libs) is installed
# separately — see README: `pip install -e libs` from the repo root.

fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
pillow==12.3.0
pydantic==2.13.5
httpx==0.28.1
arq==0.28.0
redis==5.3.1
slowapi==0.1.10
# HEIC/HEIF decoding (iPhone's default photo format), registered in
# app/pipeline.py. Same small decode-only package ultralytics used to
# pip-install at runtime on demand — pinned here instead.
pi-heif==1.4.0

# ktp-schema (the shared contracts package in ../../libs) is installed
# separately — see README: `pip install -e libs` from the repo root.

-r requirements.txt
pytest==9.1.1
httpx==0.28.1
# ktp_ocr/text_lines.py imports numpy at module level. It normally arrives
# via `pip install -e services/ocr` (pyproject.toml), but that also pulls in
# all of PaddleOCR/PaddlePaddle — listing it here lets the test suite run
# without them (as CI does). Keep in sync with pyproject.toml's pin.
numpy==2.4.6
